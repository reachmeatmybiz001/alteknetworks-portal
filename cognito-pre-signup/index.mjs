// Self-registration is intentionally open to any valid email address.
// Approval is enforced after registration: only users approved by a Super Admin
// receive a portal group/customer association and can access the support portal.
export const handler = async (event) => event
